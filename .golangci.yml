version: "2"

linters:
  default: standard
  settings:
    errcheck:
      # Renderers write to buffers/stdout; a failed write there is not
      # actionable.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
